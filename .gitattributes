# Normalkan akhir baris: repo selalu menyimpan LF.
* text=auto

# Berkas yang dijalankan di Linux WAJIB LF, kalau CRLF akan gagal jalan.
*.sh text eol=lf
*.service text eol=lf
*.conf text eol=lf
deploy/** text eol=lf
